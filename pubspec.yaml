name: ncryptor_ui
description: "internal prototype"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  file_picker: ^10.1.2
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  permission_handler: ^12.0.0+1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
